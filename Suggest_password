# Suggest password in python

import random
import string

password_len=12
password=""
suggest_password=string.digits+string.punctuation+string.ascii_lowercase+string.ascii_uppercase
for i in range(password_len):
    password+=random.choice(suggest_password)
print("suggested password: ",password)
